/**
 * Test factories — barrel export.
 *
 * Factories produce realistic, fully-typed fixtures for use across backend
 * and front test suites. Each factory accepts an `overrides` object so tests
 * can override only the fields that matter for the assertion.
 */
export * from './project';
export * from './user';
export * from './ai';
