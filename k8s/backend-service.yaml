apiVersion: v1
kind: Service
metadata:
  name: starteria-backend
  namespace: starteria
  labels:
    app: starteria-backend
spec:
  selector:
    app: starteria-backend
  ports:
  - protocol: TCP
    port: 3001
    targetPort: 3001
  type: ClusterIP
